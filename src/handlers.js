// handlers.js
// import { rest } from "msw";
// import { rest } from "msw";

// export const handlers = [
//   rest.get("/api/data", (req, res, ctx) => {
//     return res(ctx.json({ message: "Mocked data" }));
//   }),
// Add more handlers for different API routes as needed
// ];
